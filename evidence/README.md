# Generated evidence

The signed producer creates `current.json`, hourly and transition checkpoints,
and sanitized incident records on the `evidence` branch. No unsigned placeholder
is committed to `main`.
